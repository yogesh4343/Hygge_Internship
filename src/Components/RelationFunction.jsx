import React from 'react'
import NavLinks from '../Assests/NavLinks'
import HomeContainer from './HomeContainer'
import NavBar from './NavBar'

const RelationFunction = () => {
    let heading = "Relation And Functions (Mathematics)"
    return (
      <div className="">
          <HomeContainer heading={heading} />
      </div>
  )
}

export default RelationFunction
